const {welcome, goodbye, tell } = require("../src/utils/fortune-teller");


//const question =

function Box() {
    // const promise = welcome();
    // const tellPromise = tell(question);

    // tellPromise
    // .then((fortune) => {
    //     console.log(question);
    //     console.log(fortune);
    // })
    // .catch(console.error);

    // return (<h3>"click the button again for a new quote"</h3> );
}

export default Box;